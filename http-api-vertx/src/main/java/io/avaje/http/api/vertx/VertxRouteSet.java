package io.avaje.http.api.vertx;

import io.vertx.ext.web.Router;

/**
 * A generated route set that can register routes against a Vert.x {@link Router}.
 */
public interface VertxRouteSet {

  void register(Router router);
}
