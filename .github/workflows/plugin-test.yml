name: Build

on:
  workflow_dispatch: { }
  pull_request:
    paths:
      - openapi-maven-plugin/**
jobs:
  build:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        java_version: [11, 25]
        os: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v6
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java_version }}
          distribution: "zulu"
      - name: Maven cache
        uses: actions/cache@v5
        env:
          cache-name: maven-cache
        with:
          path: ~/.m2
          key: build-${{ env.cache-name }}
      - name: Maven version
        run: mvn --version
      - name: Build with Maven
        env:
          JAVA_VERSION: ${{ matrix.java_version }}
        run: |
          cd openapi-maven-plugin && mvn clean package
